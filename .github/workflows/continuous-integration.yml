name: Continuous Integration
on: push

jobs:
  buildTestDeploy:
    name: Build Test Deploy
    runs-on: ubuntu-latest
    container:
      image: alpine
      options: --entrypoint /bin/bash --workdir /etc --env GITHUB_WORKSPACE=/data
    steps:
      - run: env
      - run: pwd
      - run: ls
      - name: checkout
        uses: actions/checkout@v2.3.3
      - name: trivial
        run: |
          ls